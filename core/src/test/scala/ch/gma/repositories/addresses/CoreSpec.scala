package ch.gma.repositories.addresses

import org.scalatest._
import flatspec._
import matchers._

class CoreSpec extends AnyFlatSpec with should.Matchers {

  "A check" should "Pass correctly" in {
    2 should be (2)
  }
}
